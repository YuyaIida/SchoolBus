# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SchoolBus' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UISample

  target 'SchoolBusTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SchoolBusUITests' do
    inherit! :search_paths
    # Pods for testing
  end

	pod 'FMDB'
	pod 'Alamofire', '~> 4.5'
end
